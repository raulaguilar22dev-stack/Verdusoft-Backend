[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: tests unitarios (sin dependencias externas)
    integration: tests de integracion (con TestClient y mocks)
    slow: tests que tardan mas de 1 segundo
